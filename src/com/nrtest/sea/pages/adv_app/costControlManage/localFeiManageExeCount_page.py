# -*- coding: utf-8 -*-"""@author: 郭春彪@license: (C) Copyright 2018, Nari.@file: localFeiManageExeCount_page.py@time: 2018/8/22 0022 15:39@desc:"""from com.nrtest.common.base_page import Page# 高级应用→费控管理→本地费控→本地费控执行统计:费控情况统计class LocalFeiManageExeCount_dis_count_Page(Page):    # 工单类型    def inputSel_app_type(self, option):        self.clean_label(option)        self.selectDropDown(option, is_multi_tab=True, is_multi_elements=True)    # 费控用户类型    def inputSel_fee_ctrl_type(self, option):        self.clean_label(option)        self.selectDropDown(option, is_multi_elements=True, is_multi_tab=True)    # 接收时间    def inputDt_receive_time(self, value):        self.clean_label(value)        self.inputDate(value)    # 结束时间    def inputDt_end_time(self, value):        self.clean_label(value)        self.inputDate(value)    # 查询    def btn_qry(self):        self.btn_query(True)# 高级应用→费控管理→本地费控→本地费控执行统计: 费控情况明细class LocalFeiManageExeCount_dis_detail_Page(Page):    # 工单类型    def inputSel_app_type(self, option):        self.clean_label(option)        self.selectDropDown(option, is_multi_tab=True, is_multi_elements=True)    # 费控用户类型    def inputSel_fee_ctrl_type(self, option):        self.clean_label(option)        self.selectDropDown(option, is_multi_tab=True, is_multi_elements=True)    # 执行状态    def inputSel_exec_status(self, option):        self.clean_label(option)        self.selectDropDown(option)    # 接收时间    def inputDt_receive_time(self, value):        self.clean_label(value)        self.inputDate(value)    # 结束时间    def inputDt_end_time(self, value):        self.clean_label(value)        self.inputDate(value)    # 工单编号    def inputStr_app_no(self, value):        self.curr_input(value, is_multi_tab=True)    # 用户编号    def inputStr_cons_no(self, value):        self.curr_input(value, is_multi_tab=True)    # 查询    def btn_qry(self):        self.btn_query(True)