# -*- coding: utf-8 -*-"""@author: 韩笑@license: (C) Copyright 2018, Nari.@file: tmnlonlinestat_page.py@time: 2019-02-02 11:05:12@desc:"""from com.nrtest.common.base_page import Page# 运行管理→采集信道管理→运行情况监测:终端在线率统计class TmnlOnlineStatPage(Page):    # 查询日期    def inputDt_query_date(self, value):        self.inputDate(value)    # 运营商    def inputSel_operator(self, option):        self.selectDropDown(option)    # 终端厂家    def inputSel_tmnl_factory(self, options):        self.selectCheckBox(options)    # 用户类型    def inputSel_cons_type(self, options):        self.selectCheckBox(options)    # 采集方式    def inputSel_coll_mode(self, options):        self.selectCheckBox(options)    # 终端类型    def inputSel_tmnl_type(self, options):        self.selectCheckBox(options)    # 终端规约    def inputSel_tmnl_protocol(self, options):        self.selectCheckBox(options)    # 统计类型    def inputChk_stat_type(self, option):        self.clickRadioBox(option)    # 查询    def btn_qry(self):        self.btn_query()# 运行管理→采集信道管理→运行情况监测:终端在线明细class TmnlOnlineDetailPage(Page):    # 查询日期    def inputDt_query_date(self, value):        self.inputDate(value)    # 实时    def inputChk_real_time(self, options):        self.clickSingleCheckBox(options)    # 采集方式    def inputSel_coll_mode(self, options):        self.selectCheckBox(options, is_multi_tab=True, is_multi_elements=True)    # 运营商    def inputSel_operator(self, option):        self.selectDropDown(option, is_multi_tab=True, is_multi_elements=True)    # 当前状态    def inputSel_cur_status(self, option):        self.selectDropDown(option)    # 终端逻辑地址    def inputStr_tmnl_logic_addr(self, value):        self.input(value)    # 终端规约    def inputSel_tmnl_protocol(self, options):        self.selectCheckBox(options, is_multi_tab=True, is_multi_elements=True)    # 终端厂家    def inputSel_tmnl_factory(self, options):        self.selectCheckBox(options, is_multi_tab=True, is_multi_elements=True)    # 用户类型    def inputSel_cons_type(self, options):        self.selectCheckBox(options, is_multi_tab=True, is_multi_elements=True)    # 终端类型    def inputSel_tmnl_type(self, options):        self.selectCheckBox(options, is_multi_tab=True, is_multi_elements=True)    # 线路编号    def inputStr_line_no(self, value):        self.input(value)    # 查询    def btn_qry(self):        self.btn_query(True)